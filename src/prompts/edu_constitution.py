"""헌법(Constitution) 작성 지시문 모음.

Edu Agent 가 헌법 7개 항목(①~⑦)을 순차 주입 방식으로 작성할 때,
각 단계의 LLM 호출에 사용하는 "작업 지시문" 빌더들을 모은다.

기획서 5.2.3 / 5.2.4 표의 "작성 원칙(Prompt Guide)" 과 "작성 형식 및 예시" 칸을
그대로 옮겨 절차적 강제(procedural constraints)로 작성한다.

순차 주입 구조:
    ①번 호출 → 결과 stage1 텍스트
    ②번 호출 (Primary Inputs = stage1) → 결과 stage2 텍스트
    ③번 호출 (Primary Inputs = stage1 + stage2) → 결과 stage3 텍스트
    ④~⑦ 도 동일한 구조로 이어진다 (Phase 2-2-C 에서 추가).
"""

from __future__ import annotations


# ============================================================
# ① 교육공학적 문제 재정의
# ============================================================


STAGE1_INSTRUCTION = """헌법 ①번 단계 — **교육공학적 문제 재정의**.

## 목적
사용자 요청을 기능 요구가 아닌 **학습 결핍 문제** 로 재정의한다.

## 작성 원칙
- 표면 요청을 그대로 옮기지 마라. 같은 문제가 반복되는 **근본 원인** 을 파악한다.
- 학습자 행동 관점으로 해석한다.
- 추상적인 불만을 구체적인 교육 문제로 정리한다.
- [Global Context] 의 사용자 입력(target_user / problem / goal / solution / 실행 제약)을 모두 참조한다.

## 작성 형식
다음 3단 추론을 그대로 따른다:

```
현재 문제 → 근본 원인 → 학습 결핍 정의
```

각 항목 1~2문장. 화살표(→) 도 그대로 사용.

## 예시 (참고용, 그대로 복사 금지)
```
학생들이 질문을 잘 못한다.
→ 질문 구조를 모른다.
→ 질문 구성 능력이 부족하다.
```
"""


STAGE1_OUTPUT_HINT = """다른 안내문·코드블록·메타 설명 없이 본문만 출력한다.
첫 줄은 "## ① 교육공학적 문제 재정의" 머리말로 시작한다."""


# ============================================================
# ② 교수 기법 탐색
# ============================================================


STAGE2_INSTRUCTION = """헌법 ②번 단계 — **교수 기법 탐색**.

## 목적
①번 단계에서 정의된 학습 결핍을 해결할 수 있는 **교수 기법 후보** 를 폭넓게 탐색한다.

## 작성 원칙 (앵커링 편향 방지 절차 — 반드시 준수)
1. **최소 3개 이상의 후보** 를 제시한다. 더 적게 제시하지 마라.
2. **익숙한 기법으로 좁히지 않는다.** 이론·학파·문화적 출처를 다양하게 가져온다.
3. 각 후보는 ①번에서 정의된 학습 결핍과 **어떻게 연결되는지** 명시한다.
4. 각 후보의 **기대 효과** 와 **MVP 적용 난이도** 를 함께 평가한다.
5. **MVP 적용 난이도 기준:**
   - 낮음: 프롬프트 중심 구현 가능
   - 중간: 간단한 상태 관리 필요
   - 높음: 복잡한 기능/장기 운영 필요
6. 후보 간 우열을 미리 단정하지 마라. 선정은 ③번 단계의 책임이다.

## 작성 형식
다음 표 형식으로 작성한다 (마크다운 표):

| 교수 기법 | 학습 결핍과의 연결 | 기대 효과 | MVP 적용 난이도 |
|---|---|---|---|
| (기법명) | (①과 어떻게 연결되는가) | (구체적 효과) | 낮음/중간/높음 |

표 아래에 각 기법의 출처/근거(이론서·실험·논문·실무 사례 중 무엇에 기반하는지)를 짧게 부연한다."""


STAGE2_OUTPUT_HINT = """다른 안내문·코드블록·메타 설명 없이 본문만 출력한다.
첫 줄은 "## ② 교수 기법 탐색" 머리말로 시작한다."""


# ============================================================
# ③ MVP 교수 기법 선정
# ============================================================


STAGE3_INSTRUCTION = """헌법 ③번 단계 — **MVP 교수 기법 선정**.

## 목적
②번 단계에서 탐색된 후보 중에서 **MVP 구현에 가장 적합한 1~2개** 를 최종 확정한다.

## 작성 원칙
- 교육 효과를 우선하되, 동시에 **구현 가능성** 을 함께 고려한다.
- [Global Context] 의 실행 제약(마감 / 인원 / 역량 / 자산) 안에서 구현 가능한 수준이어야 한다.
- ②번 후보 중 **1~2개만 채택** 한다. 3개 이상 채택은 MVP 범위 초과로 금지.
- 채택 기법과 **채택한 이유** 를 명시한다.
- 제외 기법과 **제외한 이유** 를 모두 기록한다 (반드시 ②의 모든 후보를 다룰 것).

## 작성 형식

```
### 채택 기법
- (기법명): (채택 이유 — 학습 효과 측면 + 구현 가능성 측면)

### 제외 기법
- (기법명): (제외 이유)
- (기법명): (제외 이유)
- ...
```
"""


STAGE3_OUTPUT_HINT = """다른 안내문·코드블록·메타 설명 없이 본문만 출력한다.
첫 줄은 "## ③ MVP 교수 기법 선정" 머리말로 시작한다."""


# ============================================================
# ④ 서비스 전체 설계 원칙 (헌법 본체 시작)
# ============================================================


STAGE4_INSTRUCTION = """헌법 ④번 단계 — **서비스 전체 설계 원칙**.

이 단계부터(④~⑦)는 후속 산출물 9종이 직접 참조하는 **헌법 본체**다. 추상 가치 선언이 아니라
서비스 운영에 직접 활용 가능한 기준으로 작성한다.

## 목적
③번 단계에서 선택된 교수 기법의 핵심 원리를 **서비스의 실제 동작 원칙** 으로 번역한다.
⑤(학습 목표) / ⑥(평가 루브릭) / ⑦(서비스 플로우) 설계 시의 기준이 된다.

## 작성 원칙
1. **추상 가치 선언 금지.** 각 원칙은 기능 우선순위·피드백 방식·사용자 흐름 판단에 직접 활용 가능해야 한다.
2. ③번에서 채택된 교수 기법의 **핵심 원리가 자연스럽게 드러나야** 한다.
3. [Global Context] 의 실행 제약 안에서 **실행 가능한 수준** 으로 작성한다.
4. **3~5개의 핵심 원칙** 으로 간결하게 정리한다. 더 많거나 적게 두지 마라.
5. **피드백 방식** 을 반드시 명시한다:
   - 순차적 보완 (한 번에 하나씩 차례로) / 병렬적 보완 (한 번에 전체)
   - 어느 쪽을 선택했고, ③번 채택 기법의 어떤 원리에서 그 선택이 비롯되었는지 명시.

## 작성 형식

```
1. (원칙 한 문장)
2. (원칙 한 문장)
3. (원칙 한 문장)
...

**피드백 방식:** 순차 / 병렬
**선택 근거:** (③번 채택 기법의 어떤 원리에서 비롯되었는가)
```

각 원칙은 한 문장으로. 부연 설명이 필요하면 다음 줄에 들여쓰기로 1줄 추가."""


STAGE4_OUTPUT_HINT = """다른 안내문·코드블록·메타 설명 없이 본문만 출력한다.
첫 줄은 "## ④ 서비스 전체 설계 원칙" 머리말로 시작한다."""


# ============================================================
# ⑤ 학습 목표 (Learning Objective)
# ============================================================


STAGE5_INSTRUCTION = """헌법 ⑤번 단계 — **학습 목표(Learning Objective)**.

## 목적
사용자가 서비스 이용 후 **달성해야 할 변화** 를 정의한다.
⑥번 평가 루브릭의 모든 평가축은 이 학습 목표에서 분해된다.

## 작성 원칙
1. 추상적 역량·태도 목표는 사용 가능하다. 단, **반드시 관찰 가능한 행동 목표로 함께 구체화** 한다.
2. 서비스 내에서 **판단 가능한 수준** 으로 작성한다 (서비스가 측정/감지할 수 있어야 함).
3. 권장 형태: **조건 + 행동 + 성취 수준**.
4. ④번 설계 원칙과 ③번 채택 기법의 핵심 원리를 반영한다.
5. [Global Context] 의 target_user 의 인지 발달 수준에 맞춘다.

## 작성 형식

```
**학습 목표:**
(조건) + (행동) + (성취 수준) 형태의 한~두 문장.

**관찰 가능한 행동 지표:**
- (행동 지표 1)
- (행동 지표 2)
- ...
```

## 예시 (참고용, 그대로 복사 금지)
"학생이 AI에게 질문할 때, 주제 / 상황 / 원하는 답 형태를 포함해
스스로 질문을 작성할 수 있다."
"""


STAGE5_OUTPUT_HINT = """다른 안내문·코드블록·메타 설명 없이 본문만 출력한다.
첫 줄은 "## ⑤ 학습 목표" 머리말로 시작한다."""


# ============================================================
# ⑥ 평가 루브릭 (Rubric)
# ============================================================


STAGE6_INSTRUCTION = """헌법 ⑥번 단계 — **평가 루브릭(Rubric)**.

## 목적
⑤번 학습 목표의 **달성 수준을 판단하는 기준** 을 만든다.
⑦번 서비스 플로우의 시스템 판단 분기는 이 루브릭의 결과를 조건값으로 사용한다.

## 작성 원칙
1. ⑤번 학습 목표를 **3개 평가축** 으로 분해한다. 더 많거나 적게 두지 마라.
2. 평가축 이름은 추상 개념어 사용 가능 (예: 구체성, 맥락성, 목적성).
3. 각 평가축은 **사용자의 실제 입력·행동을 기준으로 판정 가능한 관찰 지표** 를 포함한다.
4. 각 평가축마다 **우수 / 양호 / 미흡** 3단계 수준을 작성한다.
5. 각 수준마다 **사용자가 보일 수 있는 예시** 를 함께 제시한다 (target_user 어휘로).
6. **최소 성취 기준** 을 명시한다 (학습 목표 달성으로 인정되는 최저선).
7. **최소 성취 기준 설정 이유** 는 ④번 설계 원칙과 연결해서 작성한다.

## 작성 형식

```
| 평가 기준 | 우수 | 양호 | 미흡 |
|---|---|---|---|
| **(축 1 이름)** | (우수 정의 + 예시) | (양호 정의 + 예시) | (미흡 정의 + 예시) |
| **(축 2 이름)** | ... | ... | ... |
| **(축 3 이름)** | ... | ... | ... |

**최소 성취 기준:** (구체 기준)
**설정 이유:** (④번의 어떤 원칙에서 비롯되었는가)
```

마크다운 표 형식을 정확히 지킬 것. 표 셀 안에서 줄바꿈이 필요하면 `<br>` 사용.
예시 문구는 target_user 가 실제 쓸 법한 자연스러운 언어로."""


STAGE6_OUTPUT_HINT = """다른 안내문·코드블록·메타 설명 없이 본문만 출력한다.
첫 줄은 "## ⑥ 평가 루브릭" 머리말로 시작한다.
표는 GitHub Flavored Markdown 표 문법을 그대로 사용한다."""


# ============================================================
# ⑦ 루브릭 기반 서비스 플로우 원칙
# ============================================================


STAGE7_INSTRUCTION = """헌법 ⑦번 단계 — **루브릭 기반 서비스 플로우 원칙**.

## 목적
⑥번 루브릭을 활용해 각 상황에서 **서비스가 어떻게 동작할지** 정의한다.
이 산출물은 후속 단계의 state_machine / data_schema / interface_spec 의 직접 근거가 된다.

## 작성 원칙
1. ⑥번 루브릭의 평가 결과를 **조건값** 으로 사용한다.
2. ④번 피드백 방식(순차/병렬)에 따라 평가 구조를 선택한다.
   - **순차 선택 시:** 루브릭 기준의 판단 순서와 그 순서를 정한 교육적 근거를 함께 작성.
   - **병렬 선택 시:** 모든 기준을 동시 판단하는 근거를 작성.
3. **최소 성취 기준 미달 시:** 부족한 항목을 보완하는 흐름을 정의한다.
4. **최소 성취 기준 충족 시:** 다음 학습 단계 / 다음 기능 / 완료 단계 중 적절한 흐름으로 전환한다.
5. 각 흐름은 **시스템 판단(내부 결정)** 과 **시스템 출력(사용자에게 보여지는 것)** 을 구분해 작성한다.
6. ④번 설계 원칙에 부합하며 **실제 구현 가능한 수준** 으로 작성한다.

## 작성 형식

```
**평가 방식:** 순차 / 병렬
**선택 근거:** (④의 어떤 원칙에서 비롯되었는가)

[순차 선택 시에만 작성]
**판단 순서:** (예: 구체성 → 맥락성 → 목적성)
**판단 순서 근거:** (해당 순서를 선택한 교육적 논리)

| 조건 | 시스템 판단 | 시스템 출력 | 사용자 다음 경험 |
|---|---|---|---|
| (조건 1) | (내부 결정) | (사용자에게 보이는 것) | (다음 행동/대기) |
| ... | ... | ... | ... |
```

마크다운 표 형식을 정확히 지킬 것.
표는 ⑥번 루브릭의 모든 평가축의 양호/미흡 분기 + 최소 성취 충족 케이스 + 예외 케이스를
빠짐없이 포함해야 한다."""


STAGE7_OUTPUT_HINT = """다른 안내문·코드블록·메타 설명 없이 본문만 출력한다.
첫 줄은 "## ⑦ 루브릭 기반 서비스 플로우 원칙" 머리말로 시작한다.
표는 GitHub Flavored Markdown 표 문법을 그대로 사용한다."""
