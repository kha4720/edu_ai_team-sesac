"""Build Plan 작성 지시문.

기획서 5.3.4 표를 그대로 옮긴다. Tech Agent 가 사용한다.
"""

from __future__ import annotations


BUILD_PLAN_INSTRUCTION = """**Build Plan** 을 작성한다.

## 한 줄 정의
MVP 기능을 실제로 구현하기 위한 최소 개발 계획 문서.

## 작성 원칙
- MVP Scope 의 **필수 기능만** 구현 대상으로 선정한다.
- User Flow 가 실제로 동작하도록 **필요한 최소 모듈 중심** 으로 설계한다.
- 각 구현 모듈은 **역할 / 입력값 / 출력값 / 연결되는 User Flow 단계** 가 드러나도록 작성한다.
- 필요 API / AI 기능은 단순 나열하지 않고, **"무엇을 입력받아 무엇을 생성하는지"** 를 함께 작성한다.
- [Global Context] 의 실행 제약(마감 / 인원 / 역량 / 자산) 을 고려해 **현실적 구현 범위** 를 설정한다.
- 기술 방식은 서비스 목적과 팀 역량에 맞는 **최소 복잡도의 스택** 으로 선정한다.
- 구현 순서는 **사용자 체감 가치 + 선행 의존성** 을 함께 고려해 우선순위를 정한다.
- 완료 기준은 **실제 시연 가능 여부 + 핵심 기능 동작 여부** 중심으로 설정한다.

## 작성 형식 (정확히 이 순서대로 머리말 6개를 사용)
```
# Build Plan

## 1. 구현 모듈
### A. (모듈명)
- 역할:
- 입력값:
- 출력값:
- 연결 User Flow:

### B. (모듈명)
- 역할:
- 입력값:
- 출력값:
- 연결 User Flow:

(...)

## 2. 필요 API / AI 기능
### A. (API/AI 기능명)
- 입력값:
- 출력값:
- 목적:

(...)

## 3. 기술 방식
- Frontend:
- Backend:
- AI:
- 통신 방식:
- 실행 환경:

## 4. 구현 순서
1) (단계)
2) (단계)
...

## 5. 담당 역할
- 프론트엔드:
- 백엔드:
- 기획/콘텐츠:

## 6. 완료 기준
- (기준 1)
- (기준 2)
...
```
"""


BUILD_PLAN_OUTPUT_HINT = """다른 안내문·코드블록·메타 설명 없이 본문만 출력한다.
첫 줄은 정확히 "# Build Plan" 머리말로 시작한다.
다음 6개 머리말이 **정확히 이 순서대로** 모두 등장해야 한다:
  1) "## 1. 구현 모듈"
  2) "## 2. 필요 API / AI 기능"
  3) "## 3. 기술 방식"
  4) "## 4. 구현 순서"
  5) "## 5. 담당 역할"
  6) "## 6. 완료 기준"
순서·번호·텍스트가 모두 정확히 일치해야 한다."""
