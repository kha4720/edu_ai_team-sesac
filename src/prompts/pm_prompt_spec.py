"""Prompt Spec 작성 지시문 (Prompt Agent 용).

기획서 5.4 / artifact_schema.PROMPT_SPEC 기준.
"""

from __future__ import annotations


PROMPT_SPEC_INSTRUCTION = """**Prompt Spec** 을 작성한다.

## 한 줄 정의
헌법의 교육 설계 원칙을 LLM 이 실제로 따를 수 있는 프롬프트 언어로 번역한 문서.

## 작성 원칙 — 헌법 매핑
[Primary Inputs] 의 헌법 각 항목을 다음과 같이 프롬프트 구성요소로 변환한다:
- **헌법 ④ 서비스 전체 설계 원칙** → 시스템 프롬프트의 **Constraints** (금지·제약 사항)
- **헌법 ⑤ 학습 목표** → 시스템 프롬프트의 **Role** (LLM 이 맡는 역할 정의)
- **헌법 ⑥ 평가 루브릭** → 시스템 프롬프트의 **판단 로직** (평가 기준과 점수 체계)
- **헌법 ⑦ 루브릭 기반 서비스 플로우 원칙** → **모드별 분기** (mode 값별 응답 전략)

## 출력 필드 일치 규칙
- 각 모드별 프롬프트의 출력 필드는 [Secondary Inputs] 의 Data Schema `output` 필드와 **정확히 일치**해야 한다.
- few-shot 예시의 응답도 동일한 출력 필드 구조를 따른다.

## Few-shot 작성 규칙
- 모드별로 **최소 1개** 이상의 few-shot 예시를 작성한다.
- 각 few-shot 은 `User:` / `Assistant:` 쌍으로 구성한다.
- 예시는 실제 서비스 타겟 사용자의 전형적인 입력을 사용한다.

## 작성 형식 (정확히 이 머리말 4개를 모두 사용)
```
# Prompt Spec

## 1. 공통 시스템 프롬프트
### Role
(헌법 ⑤ → LLM 역할 정의)

### Constraints
(헌법 ④ → 제약 사항 목록)

### 판단 로직
(헌법 ⑥ → 평가 기준과 판단 방법)

## 2. 모드별 프롬프트
각 mode 값에 따른 응답 전략과 추가 지시.

### mode: (값1)
(응답 전략, 피드백 방식, 출력 형식)

### mode: (값2)
...

## 3. Few-shot 예시 모음
각 mode 별 예시 최소 1개.

### mode: (값1) 예시
User: ...
Assistant: ...

## 4. 출력 형식 정의
LLM 이 반환해야 하는 필드 목록 (data_schema output 과 일치).

| 필드명 | 타입 | 설명 |
|--------|------|------|
```
"""


PROMPT_SPEC_OUTPUT_HINT = """다른 안내문·코드블록·메타 설명 없이 본문만 출력한다.
첫 줄은 정확히 "# Prompt Spec" 머리말로 시작한다.
다음 4개 머리말이 **정확히 이 순서대로** 모두 등장해야 한다:
  1) "## 1. 공통 시스템 프롬프트"
  2) "## 2. 모드별 프롬프트"
  3) "## 3. Few-shot 예시 모음"
  4) "## 4. 출력 형식 정의"
순서·번호·텍스트가 모두 정확히 일치해야 한다."""
