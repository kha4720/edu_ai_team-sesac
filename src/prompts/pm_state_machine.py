"""State Machine 작성 지시문.

기획서 5.4 / artifact_schema.STATE_MACHINE 기준. PM Agent 가 사용한다.
"""

from __future__ import annotations


STATE_MACHINE_INSTRUCTION = """**State Machine** 을 작성한다.

## 한 줄 정의
서비스의 각 상태와 상태 간 전이 조건, 예외 처리를 정의한 비즈니스 로직 문서.

## 작성 원칙
- **헌법 ⑦ 루브릭 기반 서비스 플로우 원칙** 에서 시스템 판단 구조를 상태값으로 구체화한다.
- 상태 전이 규칙은 **IF-THEN** 구조로 작성한다.
- [Secondary Inputs] 의 Data Schema 에 정의된 `output.mode` 값과 **1:1로 대응**되는 상태가 반드시 존재해야 한다.
- 모든 분기(양호/미흡/예외)를 빠짐없이 명시한다. 분기 누락 시 서비스 로직에 구멍이 생긴다.
- [Primary Inputs] 의 User Flow 화면 전환 조건을 상태 전이 트리거로 활용한다.
- [Primary Inputs] 의 QA Plan 예외 케이스를 예외 처리 및 에러 메시지에 반영한다.

## 작성 형식 (정확히 이 머리말 5개를 모두 사용)
```
# State Machine

## 1. 상태 목록
| 상태명 | 설명 | 진입 조건 |
|--------|------|-----------|
| (상태명) | (설명) | (진입 조건) |

## 2. 상태 전이 규칙
각 상태에서 가능한 전이를 IF-THEN 구조로 기술한다.

### (상태명)
- IF (조건): THEN → (다음 상태)
- IF (조건): THEN → (다른 상태)

## 3. data_schema mode 값 매핑 규칙
output.mode 값과 상태의 1:1 대응표.

| mode 값 | 대응 상태 | 설명 |
|---------|----------|------|

## 4. 예외 처리 및 에러 메시지
| 예외 상황 | 에러 메시지 | 전이 상태 |
|----------|------------|----------|

## 5. data_schema mode 값 매핑
(mode 값과 UI 렌더링·피드백 분기 전체를 표로 정리)
```
"""


STATE_MACHINE_OUTPUT_HINT = """다른 안내문·코드블록·메타 설명 없이 본문만 출력한다.
첫 줄은 정확히 "# State Machine" 머리말로 시작한다.
다음 5개 머리말이 **정확히 이 순서대로** 모두 등장해야 한다:
  1) "## 1. 상태 목록"
  2) "## 2. 상태 전이 규칙"
  3) "## 3. data_schema mode 값 매핑 규칙"
  4) "## 4. 예외 처리 및 에러 메시지"
  5) "## 5. data_schema mode 값 매핑"
순서·번호·텍스트가 모두 정확히 일치해야 한다."""
